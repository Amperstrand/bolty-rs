[pytest]
# importlib mode: difftest/tests/test_difftest.py and tests/test_difftest.py
# are DIFFERENT suites sharing a basename — rootdir imports collided.
import-mode = importlib
addopts = --timeout=120 --timeout-method=thread
